import HeroSection from "../components/HeroSection";

const Home = ({ multipleSectionData }) => {
  return (
    <div>
      <HeroSection multipleSectionData={multipleSectionData} />
    </div>
  );
};

export default Home;
